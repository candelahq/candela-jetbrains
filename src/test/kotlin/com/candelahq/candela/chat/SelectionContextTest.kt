package com.candelahq.candela.chat

import org.junit.jupiter.api.Test

/**
 * Unit tests for [SelectionContext] data class.
 *
 * SelectionContext relies on Document + RangeMarker which require
 * the IntelliJ platform. Integration tests live in [SmartReplacePlatformTest].
 * This file serves as a placeholder for pure-unit-testable utilities
 * that may be extracted from SelectionContext in the future.
 */
class SelectionContextTest {
    @Test
    fun `data class exists and is importable`() {
        // Verifies that SelectionContext compiles and is accessible.
        // Functional tests require the IntelliJ platform and live in
        // SmartReplacePlatformTest.
    }
}
